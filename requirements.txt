fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
pypdf==4.3.1
python-docx==1.1.2
numpy==1.26.4
python-dotenv==1.0.1
email-validator==2.2.0

# Postgres + pgvector: required for DATABASE_URL=postgresql+psycopg://...
# (Supabase, Railway Postgres, or any other Postgres). See database.py's
# IS_POSTGRES branching and README.md -> "Migrating to Postgres + pgvector".
# Harmless to have installed even when running locally against SQLite.
# psycopg (v3), not psycopg2 -- matches the "+psycopg" driver segment in
# the DATABASE_URL scheme (SQLAlchemy resolves the driver from that
# segment; IS_POSTGRES itself only checks engine.dialect.name, which is
# "postgresql" either way, so nothing else in the app cares which psycopg
# generation is installed).
psycopg[binary]==3.2.3
pgvector==0.3.4

# Testing
pytest==8.3.3
httpx==0.27.2
